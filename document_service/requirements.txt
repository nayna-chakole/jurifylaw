# --- Web service ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9

# --- LLM providers (for summary/obligations/reasoning generation) ---
groq>=0.9,<1.0
openai>=1.30,<2.0
google-genai>=1.0
python-dotenv>=1.0,<2.0

# --- Document processing ---
pymupdf>=1.24
python-docx>=1.1
pytesseract>=0.3.10
Pillow>=10.0

# --- NER ---
spacy>=3.7,<4.0

# --- Clause risk classification ---
transformers>=4.34,<5.0
torch>=2.0
huggingface_hub<1.0,>=0.34.0

# NOTE: same as ai_service/requirements.txt — install CPU-only torch
# FIRST to avoid pulling a large CUDA build nobody needs:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# NOTE: this service also requires:
#   1. Tesseract OCR installed separately (not a pip package) —
#      https://github.com/UB-Mannheim/tesseract/wiki (Windows installer)
#   2. spaCy's English model downloaded separately:
#      python -m spacy download en_core_web_sm